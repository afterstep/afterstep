<varlistentry id="options.DefaultFrame">
	<term>DefaultFrame <emphasis remap='I'>"name"</emphasis></term>
	<listitem>
		<para>MyFrame to use as default for all windows.
		Note: if you want a different framing for any window,
		you need to specify it in the  database  file using the Frame
		keyword.</para>
	</listitem>
</varlistentry>
