<varlistentry id="options.Path">
	<term>Path <emphasis remap='I'>path:path:path</emphasis></term>
	<listitem>
		<para>Specifies the search paths for icons (IconPath), fonts (FontPath), pixmaps (PixmapPath), cursors (CursorPath) and scripts (ScriptPath). Multiple entries for a given search path are separated by a colon.</para>
	</listitem>
</varlistentry>
